{
  "name": "stephany-ignite-ui",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build --filter=@stephany-ignite-ui/docs && changeset publish"
  },
  "packageManager": "npm@9.8.0",
  "devDependencies": {
    "@changesets/cli": "^2.29.1",
    "turbo": "^2.5.0"
  }
}